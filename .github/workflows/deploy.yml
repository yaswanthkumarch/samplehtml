# name: Deploy File to VM

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup SSH
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
#           chmod 600 ~/.ssh/id_ed25519
#           ssh-keyscan -H 143.110.252.102 >> ~/.ssh/known_hosts

#       - name: Create file on VM
#         run: |
#           ssh root@143.110.252.102 "echo 'File created from GitHub Actions at $(date)' > /home/one.txt"

name: Deploy Static HTML to VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Deploy files via SSH
      - name: Deploy to VM
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          port: 22
          script: |
            # Clean old files
            sudo rm -rf /var/www/html/*

            # Copy all files from repo to web root
            sudo cp -r * /var/www/html/

            # Set proper permissions
            sudo chown -R ${{ secrets.VM_USER }}:www-data /var/www/html
            sudo chmod -R 775 /var/www/html

            # Reload Nginx
            sudo systemctl reload nginx
